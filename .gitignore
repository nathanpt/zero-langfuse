# Binaries
/zero-langfuse
/bin/
/dist/

# Local config / credentials (never commit)
config.json
*.local

# Editor / OS
.DS_Store
*.swp

# Node (npm wrapper has no deps; local tooling may create these)
node_modules/
*.tgz
